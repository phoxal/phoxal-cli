name: release-prep

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: release-prep
  cancel-in-progress: false

jobs:
  prepare:
    uses: phoxal/organization/.github/workflows/rust-release-prep.yml@master
    secrets: inherit
