schema: v0
api_version: y2026_1

identity:
  id: robot-v1
  namespace: dev

structure: structure.urdf

phoxal_participants:
  channel: stable
  images:
    drive: ghcr.io/phoxal/service-drive:y2026_1-v0.13.0
    localize: ghcr.io/phoxal/service-localize:y2026_1-v0.8.0

user_participants:
  mission_behavior:
    path: ./runtimes/mission_behavior
  inspection_policy:
    path: ./runtimes/inspection_policy

motion:
  kinematic:
    kind: differential
    left_actuators: [left_drive.motor]
    right_actuators: [right_drive.motor]
    left_encoders: [left_drive.encoder]
    right_encoders: [right_drive.encoder]
    wheel_radius_m: 0.12
    wheel_base_m: 0.6

components:
  sources:
    oak_d_lite:
      git: https://github.com/phoxal/framework
      tag: main
      directory: component/oak_d_lite
    ddsm115:
      git: https://github.com/phoxal/framework
      tag: main
      directory: component/ddsm115
    passive_caster:
      path: ./components/passive_caster
  instances:
    left_drive:
      component: ddsm115
      mount_link: left_wheel_mount
      driver:
        connection: { type: can, bus: 0, node_id: 1 }
    front_camera:
      component: oak_d_lite
      mount_link: front_camera_mount
      driver:
        connection: { type: usb }
      roles:
        rgb: [localization]
        depth: [localization, mapping, traversability]
